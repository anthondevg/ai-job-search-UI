# Frontend (Vite) — copy to `.env` in the project root
# VITE_API_URL=http://localhost:3001

# Skip Gemini API calls for analyze + tailor CV; uses local dummy data instead.
VITE_DEV_MOCK_GENERATE=false
